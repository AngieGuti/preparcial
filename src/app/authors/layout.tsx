import React from 'react';
import Link from 'next/link';
import Image from 'next/image';

export default function AuthorsLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    // Acá se podrían agregar rutas específicas como ns
    return (
        
